import { AdminShell } from "@/components/admin/AdminShell";
import { markEnquiryRead } from "@/app/admin/actions";
import { requireAdmin } from "@/lib/auth";

export default async function EnquiriesAdmin() { const { supabase } = await requireAdmin(); const { data: enquiries } = await supabase.from("enquiries").select("*").order("created_at", { ascending: false }); return <AdminShell><div className="admin-title"><div><h1>Enquiries</h1><p>Quotation and contact requests submitted through the public website.</p></div></div><section className="admin-panel"><div style={{ overflowX: "auto" }}><table className="admin-table"><thead><tr><th>Status</th><th>Buyer</th><th>Product / quantity</th><th>Destination</th><th>Message</th><th>Date</th><th /></tr></thead><tbody>{(enquiries || []).map((enquiry) => <tr key={enquiry.id}><td><span className={`status ${enquiry.status}`}>{enquiry.status}</span></td><td><strong>{enquiry.name}</strong><br />{enquiry.company_name && <>{enquiry.company_name}<br /></>}{enquiry.email}<br />{enquiry.phone}</td><td>{enquiry.product_name}<br /><small>{enquiry.required_quantity}</small></td><td>{enquiry.country}{enquiry.packaging_requirement && <><br /><small>{enquiry.packaging_requirement}</small></>}</td><td>{enquiry.message || "—"}</td><td>{new Date(enquiry.created_at).toLocaleDateString("en-IN")}</td><td>{enquiry.status === "unread" && <form action={markEnquiryRead}><input name="id" type="hidden" value={enquiry.id} /><button className="admin-button secondary" type="submit">Mark read</button></form>}</td></tr>)}{!enquiries?.length && <tr><td colSpan={7}>No enquiries have been received yet.</td></tr>}</tbody></table></div></section></AdminShell>; }
