import { AdminShell } from "@/components/admin/AdminShell";
import { deleteProduct, saveProduct } from "@/app/admin/actions";
import { requireAdmin } from "@/lib/auth";

export default async function ProductsAdmin() { const { supabase } = await requireAdmin(); const { data: products } = await supabase.from("products").select("*").order("position"); return <AdminShell><div className="admin-title"><div><h1>Products</h1><p>Add, edit, deactivate, or delete product listings.</p></div></div><section className="admin-panel"><h2>Add product</h2><ProductForm /></section><section className="admin-panel"><h2>Existing products</h2><div className="admin-product-list">{(products || []).map((product) => <article className="admin-product-row" key={product.id}><img src={product.image_url} alt="" /><div><h3>{product.name} <small>({product.local_name})</small></h3><p>{product.is_active ? "Active" : "Inactive"} · {product.rate ? `${product.rate} ${product.unit || ""}` : "Rate not set"}</p><details><summary>Edit product</summary><ProductForm product={product} /></details></div><div><form action={deleteProduct}><input type="hidden" name="id" value={product.id} /><button type="submit">Delete</button></form></div></article>)}</div></section></AdminShell>; }
function ProductForm({ product }: { product?: Record<string, unknown> }) { return <form className="admin-form" action={saveProduct}><input type="hidden" name="id" value={String(product?.id || "")} /><div className="two-col"><label>Name<input name="name" required defaultValue={String(product?.name || "")} /></label><label>Local / Hindi name<input name="localName" required defaultValue={String(product?.local_name || "")} /></label><label>Slug<input name="slug" required placeholder="e.g. red-chilli-powder" defaultValue={String(product?.slug || "")} /></label><label>Sort position<input name="position" type="number" min="0" defaultValue={String(product?.position || 0)} /></label><label>Rate (optional)<input name="rate" type="number" min="0" step="0.01" defaultValue={product?.rate == null ? "" : String(product.rate)} /></label><label>Unit (optional)<input name="unit" placeholder="e.g. per kg" defaultValue={String(product?.unit || "")} /></label></div><label>Image URL<input name="imageUrl" required placeholder="Upload via Media Library, then paste the URL" defaultValue={String(product?.image_url || "")} /></label><label>Description<textarea name="description" required defaultValue={String(product?.description || "")} /></label><label><input name="isActive" type="checkbox" defaultChecked={product ? Boolean(product.is_active) : true} /> Active on public website</label><button type="submit">{product ? "Save product" : "Add product"}</button></form>; }
